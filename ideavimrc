set ignorecase
set smartcase

" Integrate with system clipboard
set clipboard=unnamedplus,unnamed

" Plugins
set surround

let mapleader = ','

nmap <leader>di /})<CR>o<ESC>may?it(<CR>`ap?it(<CR>wwzz

" Line number
set relativenumber
set number